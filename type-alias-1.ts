type UserInfo = {
    name: string;
    age: number;
    interest: string[]
};

let info: UserInfo = {
    name: 'Maxwell',
    age: 18,
    interest: ['drawing', 'programming']
};

console.log(info);
