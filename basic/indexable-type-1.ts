const dictionary: { [key: string]: string } = {
    name: 'Maxwell',
    description: 'will always 18 years old',
    reason: 'you never know.'
};

// keyof dictionary => error
// let info: keyof dictionary;
